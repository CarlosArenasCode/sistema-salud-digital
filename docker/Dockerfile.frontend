# Dockerfile usando Nginx para servir el frontend
FROM node:18-alpine AS builder
WORKDIR /app
COPY frontend ./frontend
# Aquí podrías ejecutar algún build si usas un framework (por ejemplo npm run build)

FROM nginx:alpine
COPY --from=builder /app/frontend /usr/share/nginx/html
EXPOSE 8081
CMD ["nginx", "-g", "daemon off;"]
